<?php

// Set the initial counter variable
$counter = 1;

// Repeat the loop at least once, then check the condition
do {
    // Display the current counter value
    echo "Número: $counter \n";

    // Increment the counter
    $counter++;
} while ($counter <= 5); // Check the condition after each iteration

?>
